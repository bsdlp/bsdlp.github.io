{
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "cssnano": "^6.0.0",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.0.2"
  },
  "scripts": {
    "build-css": "postcss ./styles.css -o ./compiled.css",
    "publish": "mkdir -p public && cp index.html public/ && cp favicon.ico public/ && cp ./compiled.css public/"
  }
}
